<p>
<label for="preg" class="rem">Pregunta:</label>
<input type="text" id="preg" name="quiz[pregunta]" value="<%= quiz.pregunta %>" 
<% if(!quiz.pregunta) { %>
	placeholder="Pregunta"
<% } %>
 />
</p>
<p>
<label for="resp" class="rem">Respuesta:</label>
<input type="text" id="resp" name="quiz[respuesta]" value="<%= quiz.respuesta %>" 
<% if(!quiz.respuesta) { %>
	placeholder="Respuesta"
<% } %>
 />
</p>
<p>Seleccione la materia que quiera relacionar a esta pregunta:
<select name="materia[id]">
	<% var i; for (i=0; i<materia.length; i++){ %>
		  <option value="<%= materia[i].id %>"><%= materia[i].nombre %></option>
	<% } %>
</select>
</p>
<p>La materia que tiene relacionada es:  <%= materia[parseInt(quiz.MateriumId)-1].nombre %></p>
<%=  %>

<a href="/quizes"><button type="button">Cancelar</button></a>
<input type="submit" value="Salvar">
